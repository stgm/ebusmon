[project]
name = "ebusmon"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.0",
    "pyebus",
    "pyyaml",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
